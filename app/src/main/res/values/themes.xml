<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.SSBPrep" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand colors. -->
        <item name="colorPrimary">@color/accent_blue</item>
        <item name="colorPrimaryDark">@color/background_dark</item>
        <item name="colorAccent">@color/accent_blue</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/background_dark</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/background_dark</item>
        <item name="android:textColorPrimary">@color/text_ios_primary</item>
        <item name="android:textColorSecondary">@color/text_ios_secondary</item>
    </style>

    <style name="GlassView">
        <item name="android:background">@drawable/glass_background</item>
    </style>
</resources>
