<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.SSBPrep" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand colors. -->
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorPrimaryDark">@color/bg_dark</item>
        <item name="colorAccent">@color/primary_blue</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowTranslucentStatus">true</item>

        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/bg_dark</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>

        <!-- Fix for yellowish autofill background (API 26+) -->
        <item name="android:autofilledHighlight" tools:targetApi="o">@android:color/transparent</item>
    </style>

    <style name="CustomCalendar" parent="Theme.AppCompat">
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="android:selectedWeekBackgroundColor">@color/google_green</item>
        <item name="android:focusedMonthDateColor">@color/text_primary</item>
        <item name="android:unfocusedMonthDateColor">@color/text_tertiary</item>
        <item name="android:weekSeparatorLineColor">@color/glass_border</item>
    </style>

    <style name="GlassView">
        <item name="android:background">@drawable/glass_background</item>
    </style>
</resources>
